<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Youth Mock Trial ry</title>

  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Inter, sans-serif;
      background: #FFFFFF;
      color: #1A1A1A;
      line-height: 1.7;
    }

    /* HERO SECTION */
    .hero {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 100px 60px;
      background: #F5EFE7; /* warm cream */
      position: relative;
      overflow: hidden;
    }

    /* Warm brown blobs */
    .blob {
      position: absolute;
      border-radius: 50%;
      opacity: 0.45;
      filter: blur(2px);
      animation: float 6s ease-in-out infinite;
    }

    .blob.brown {
      background: #8A6A4A; /* warm medium brown */
      width: 420px;
      height: 420px;
      top: -120px;
      left: -80px;
    }

    .blob.tan {
      background: #D8C0A8; /* warm tan */
      width: 380px;
      height: 380px;
      bottom: -140px;
      left: 40px;
      animation-delay: 2s;
    }

    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(25px); }
      100% { transform: translateY(0px); }
    }

    /* LEFT SIDE */
    .hero-left {
      max-width: 55%;
      z-index: 2;
    }

    .hero-left h1 {
      font-family: "Playfair Display", serif;
      font-size: 4em;
      font-weight: 700;
      margin-bottom: 20px;
      letter-spacing: -0.7px;
      color: #2E1F14; /* darker warm brown */
      background: transparent; /* fully transparent */
      text-shadow: 1px 1px 2px rgba(0,0,0,0.25); /* makes it pop */
    }

    .hero-left p {
      font-size: 1.3em;
      opacity: 0.85;
      margin-bottom: 12px;
    }

    .org-type {
      font-size: 1em;
      opacity: 0.6;
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-top: 8px;
    }

    /* RIGHT SIDE — LOGO ONLY (transparent) */
    .hero-right {
      position: relative;
      z-index: 2;
    }

    .logo-img {
      width: 240px; /* medium size */
      height: auto;
      display: block;
      background: transparent;
      filter: drop-shadow(0px 2px 4px rgba(0,0,0,0.15)); /* soft shadow */
    }

    /* REMOVE ANY OLD CIRCLE */
    .logo-circle {
      display: none !important;
    }

    /* CLEAN CURVED DIVIDER */
    .divider {
      width: 100%;
      height: 50px;
      background: #F5EFE7;
      border-bottom-left-radius: 100% 40px;
      border-bottom-right-radius: 100% 40px;
    }

    /* NAVIGATION */
    nav {
      background: #FFFFFF;
      border-bottom: 1px solid #E6E2DD;
      display: flex;
      justify-content: center;
      gap: 20px;
      padding: 16px 10px;
      border-radius: 0 0 20px 20px;
    }

    nav a {
      text-decoration: none;
      color: #1A1A1A;
      font-weight: 600;
      padding: 10px 18px;
      border-radius: 10px;
      transition: 0.3s ease;
    }

    nav a:hover {
      background: #F0F0F0;
      transform: translateY(-2px);
    }
  </style>
</head>

<body>

<!-- HERO SECTION -->
<div class="hero">
  <div class="blob brown"></div>
  <div class="blob tan"></div>

  <div class="hero-left">
    <h1>Youth Mock Trial ry</h1>
    <p>Empowering the next generation of legal professionals</p>
    <div class="org-type">Non Profit Organization</div>
  </div>

  <div class="hero-right">
    <img class="logo-img" src="logo.png" alt="Youth Mock Trial ry">
  </div>
</div>

<div class="divider"></div>

<nav>
  <a href="index.html">Home</a>
  <a href="about.html">About</a>
  <a href="hymt.html">HYMT</a>
  <a href="sponsors.html">Sponsors</a>
  <a href="contact.html">Contacts</a>
  <a href="join.html">Join Us</a>
  <a href="events.html">Other Events</a>
</nav>

</body>
</html>

